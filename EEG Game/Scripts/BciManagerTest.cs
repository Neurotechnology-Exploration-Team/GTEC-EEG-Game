using System.Collections;
using System.Collections.Generic;
using UnityEngine;

using Gtec.Unicorn;

public class BciManagerTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // Unicorn.GetAPIVersion();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
